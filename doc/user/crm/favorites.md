# Mints::User::Crm::Favorites

```ruby
user = Mints::User.new(mints_url, api_key, session_token)

user.update_multiple_favorites(data)

user.get_favorites(options)

user.update_favorites(id, data)
```