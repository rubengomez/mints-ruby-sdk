# Mints::User::Ecommerce::OrderStatuses

```ruby
user = Mints::User.new(mints_url, api_key, session_token)

user.get_order_statuses #=> Get order statuses.

user.get_order_status(id) #=> Get status of an order.
```