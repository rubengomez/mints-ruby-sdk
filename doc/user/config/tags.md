# Mints::User::Config::Tags

```ruby
user = Mints::User.new(mints_url, api_key, session_token)

user.get_tags #=> Get a collection of tags.

user.get_tag(id) #=>  Get a single tag.

user.create_tag(data) #=> Create a tag.

user.update_tag(id, data) #=> Update a tag info.

```