# Mints::Pub::Content::ContentBundles

```ruby
mints_pub = Mints::Pub.new(mints_url, api_key)

mints_pub.get_content_bundles(options) #=> Return a collection of content bundles

mints_pub.get_content_bundle('content-bundle-slug', options) #=> Return a single content bundle

mints_pub.get_content_page('content-bundle-slug', options) #=> [DEPRECATED] Return a single content bundle

```